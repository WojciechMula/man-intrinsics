include Makefile-common

BUILDDIR=$(TMPDIR)/man-intrinsics
MANDIR=$(BUILDDIR)/usr/share/man/man7
DEBDIR=$(BUILDDIR)

deb: $(XMLFILES)
	$(MKDIR) $(MANDIR)
	$(GENERATOR) --gzip -o $(MANDIR) --deb $(DEBDIR)
	chmod 444 ${MANDIR}/*.7*
	dpkg-deb -b $(DEBDIR) .

# vim: ft=make
